<!doctype html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="color-scheme" content="light dark">
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link webc:keep rel="stylesheet"
		href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:ital,wght@0,100..800;1,100..800&display=swap">
	<link webc:keep rel="stylesheet" href="/colors.css">
	<link webc:keep rel="stylesheet" :href="getBundleFileUrl('css')">
	<script webc:keep type="module" :src="getBundleFileUrl('js')"></script>
	<title>NexSpring Software</title>
	<style webc:keep webc:scoped>
		@layer decoration {
			body {
				background-image: linear-gradient(var(--grid-line) 1px, transparent 1px), linear-gradient(90deg, var(--grid-line) 1px, transparent 1px);
				background-size: 3.333rem 3.333rem;
				background-position: 50% 50%;

				.box {
					background-color: var(--bg-color);
					border-style: solid;
					border-width: 1px;
					border-color: var(--grid-line);
				}
			}

			:not(.box) span {
				background-color: var(--bg-color);
			}

			a {
				color: var(--color);
			}
		}

		@layer layout {
			body {
				width: 100%;
				margin: 0;
				display: flex;
				flex-direction: column;
				min-height: 100vh;
				gap: 1rem;

				header {
					position: fixed;
					width: 100vw;
				}

				header .container {
					align-self: stretch;
					height: 3rem;
					display: flex;
					justify-content: space-between;
					align-items: center;
				}

				.container {
					max-width: 60rem;
					margin-left: auto;
					margin-right: auto;
					padding-left: 1rem;
					padding-right: 1rem;
				}

				.header-offset {
					margin-top: 3rem;
				}

				main {
					flex: 1 1 auto;
					display: flex;
					flex-direction: column;
					gap: 3rem;
				}
			}

			.box>* {
				margin-left: 1rem;
				margin-right: 1rem;
			}
		}

		.copy {
			font-size: smaller;
			color: var(--fg-muted);
		}

		body {
			font-family: 'JetBrains Mono', monospace;
			background-color: var(--bg-color);
			color: var(--color);
		}

		h1 {
			font-size: larger;
			font-weight: bolder;
			display: inline-flex;
			align-items: center;
			color: var(--color);
			text-decoration: none;
			gap: 0.5rem;

			&::before {
				content: "";
				background-color: var(--accent);
				width: 0.5rem;
				height: 0.5rem;
			}
		}

		button {
			background-color: var(--bg-color);
			border-style: solid;
			border-width: 1pt;
			border-color: var(--color);
			color: var(--color);
			padding: 0.5rem;
			text-transform: uppercase;
			font-family: inherit;
			font-weight: bolder;
			cursor: pointer;
		}


		#contact {
			text-decoration: underline;
			cursor: pointer;
		}

		#copied {
			position: fixed;
			width: 100%;
			opacity: 0;
			bottom: 0rem;
			transition: visibility 0.2s, opacity 0.2s ease, bottom 0.2s ease;

			&.visible {
				opacity: 1;
				bottom: 1rem;
			}

			div {
				width: fit-content;
				padding: .5rem;
				border-radius: 5px;
				background-color: var(--primary);
				color: var(--fg-primary);
				margin: 0 auto;
			}
		}
	</style>
</head>

<body>
	<header class="box">
		<div class="topbar container">
			<a href="/">
				<h1>NexSpring Software</h1>
			</a>
			<a href="mailto:contact@nexspring.ca"><button>Inquire</button></a>
		</div>
	</header>

	<main class="container header-offset" @raw="content"></main>

	<footer class="box">
		<span class="copy">Copyright &copy; 2025 NexSpring Software Inc.</span>
	</footer>
	<copy-popup></copy-popup>
</body>

</html>
