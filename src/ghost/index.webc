<header>
	<h1><span>NexSpring Software Journal</span></h1>
	<p><span>Bite sized software innovation.</span></p>
</header>

<h2>Articles</h2>
<card-grid>
	{{#foreach posts}}
	<card-item>
		<ghost-feature-image></ghost-feature-image>
		<h3><a href="{{url}}">{{title}}</a></h3>
		<p>{{excerpt}}</p>
	</card-item>
	{{/foreach}}
</card-grid>
